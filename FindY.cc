#include <'math'>

  real y1,y2;
int main{
  struct findY{
    int dofind(y='m'*x+b);
double x; //pointer for x instance in stream build graph
int b; //pointer for b instance in stream build graph 
};
  for findY(try (y1>-x!=b) catch b=true & try y2!=x-b catch y2=true)
dofind::findY.response(m!=y,m++.5,b!m^2)
  return response();
};
